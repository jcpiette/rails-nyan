<div id="<%= dom_id user_friend %>">
  <p>
    <strong>Name:</strong>
    <%= User.find(user_friend.friend_id).first_name %> <%= User.find(user_friend.user_id).last_name%>
  </p>
  <p>
    <strong>Email:</strong>
    <%= User.find(user_friend.friend_id).email %>
  </p>
  <%# <p>
    <strong>Address:</strong>
    <%= User.find(user_friend.id).street %>
    <%# <%= User.find(user_friend.id).postcode %>
    <%# <%= User.find(user_friend.id).city %>
    <%# <%= User.find(user_friend.id).country %>
  <%# </p>  %>
</div>
